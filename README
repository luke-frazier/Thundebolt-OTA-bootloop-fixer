------------------

Only do this if you can't get into a custom recovery:

Unzip this, and run run.bat on a windows computer.

------------------

Only do this if you can get into a custom recovery:

Flash this zip through recovery.

------------------
